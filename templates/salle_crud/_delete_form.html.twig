<form method="post" action="{{ path('app_salle_crud_delete', {'id': salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.id) }}">
    <button class="btn btn-danger my-3">Supprimer</button>
</form>
